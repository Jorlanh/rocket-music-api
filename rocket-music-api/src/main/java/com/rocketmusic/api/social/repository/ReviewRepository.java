package com.rocketmusic.api.social.repository;

import org.springframework.stereotype.Repository;

@Repository
public interface ReviewRepository {
    // Pode estender JpaRepository futuramente quando a entidade Review for criada
}